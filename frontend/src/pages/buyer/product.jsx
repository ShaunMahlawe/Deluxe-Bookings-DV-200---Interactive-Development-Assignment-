// SOFT REMOVE: `/product` now renders StaysPage directly from App.jsx.
// This wrapper remains so older imports do not break while the dead file is
// cleaned up later.
import StaysPage from "../public/StaysPage";

export default StaysPage;
